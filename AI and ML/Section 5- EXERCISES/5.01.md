# **Exercise**

1) what are the 3 pricing fundamentals of the AWS cloud?
   => compute(how long it ran), storage(how much you stored), Data transfer out of the cloud(but data transfer in the cloud is free) 

2) what do we consider when choosing an AWS region?
   => compliance with data governance(compliance requirement to store data in local regions), proximity(latency), avialble services(some services are not available in some regions), pricing(pricing might differ based on region) 

3) When a model is neither underfitting nor overfitting, it will have.
   => low bias and low variance

4) Which AI application is used to automatically extract structured data from various types of documents, such as invoices, contracts, and forms?
   => Intellegent Document Processing(IDP)
5) A company develops a machine learning model to predict stock prices. The model performs well on the training data but poorly on new, unseen data. Which option describes the fit of the model?
   => it overfits  
   


