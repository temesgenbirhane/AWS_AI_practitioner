# What are Parameters and HyperParameters

![](../assets/Section2/image30.png)

**Parameters → Values the model learns automatically during training (weights, biases).**

**Hyperparameters → Settings you choose before training that control how the model learns (learning rate, batch size, number of layers).**